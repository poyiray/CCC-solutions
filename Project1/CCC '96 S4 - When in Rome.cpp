#include <bits/stdc++.h>
using namespace std;

int n;
char c;

int toNum(string s)
{
	for (int i = 0; i < s.size(); i++)
	{
		if()
	}
}

int main()
{
	cin >> n;
	while (n--)
	{
		string a, b;
		while (cin >> c && c != '+') a += c;
		while (cin >> c && c != '=') b += c;

	}

	return 0;
}